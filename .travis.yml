languaje: node_js
node_js:
  - "8"

before_script:
  - npm install
  - npm build --mode production

deploy:
  provider: pages
  skip_cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local_dir: "dist/"
  on:
    branch: master